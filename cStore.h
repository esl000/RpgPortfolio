#pragma once
#include "cUIObject.h"

#include "cItem.h"

/*
class cItem;

class cStore :
	public cUIObject
{
private:
	cItem*				m_rcSlot[5][2];
	cItem*				m_rcDest[5][2];
	cItem*				m_pTempItem;

public:
	cStore();
	virtual ~cStore();
	virtual void Setup();
	virtual void Update();
	virtual void Render(LPD3DXSPRITE pSprite);
	cItem* GetStore(int y, int x);
	cItem* GetStoreItem(int y, int x); 
	void GetTestStore(int y, int x);
	cItem* RemoveItem(int y, int x);
	cItem::eItemType StoreType(int nData);
	string StoreImage(int nData);
	void AddStore(cItem* pItem);
	void AddStore(cItem* pItem, int y, int x);
	void InitItem();
};
*/

